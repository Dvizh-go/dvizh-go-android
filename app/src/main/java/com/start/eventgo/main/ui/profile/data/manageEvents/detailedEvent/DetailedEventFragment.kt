package com.start.eventgo.main.ui.profile.data.manageEvents.detailedEvent

import android.os.Bundle
import android.view.View
import androidx.fragment.app.Fragment
import by.kirich1409.viewbindingdelegate.viewBinding
import com.start.eventgo.R
import com.start.eventgo.databinding.FragmentDetailedEventBinding

class DetailedEventFragment : Fragment(R.layout.fragment_detailed_event) {

    private val binding: FragmentDetailedEventBinding by viewBinding()

    override fun onViewCreated(view: View, savedInstanceState: Bundle?) {
        super.onViewCreated(view, savedInstanceState)
    }
}
